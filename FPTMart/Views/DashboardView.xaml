<UserControl x:Class="FPTMart.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!-- Stats Cards -->
            <UniformGrid Rows="1" Columns="4" Margin="0,0,0,25">
                <!-- Today Revenue -->
                <materialDesign:Card Margin="0,0,15,0" Padding="20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Background="#E3F2FD" CornerRadius="8" Padding="10">
                                <materialDesign:PackIcon Kind="CurrencyUsd" Width="24" Height="24" Foreground="#1976D2"/>
                            </Border>
                            <TextBlock Text="Doanh Thu Hôm Nay" Foreground="Gray" VerticalAlignment="Center" Margin="12,0,0,0"/>
                        </StackPanel>
                        <TextBlock Text="{Binding TodayRevenue, StringFormat='{}{0:N0} đ'}" FontSize="28" FontWeight="Bold" Margin="0,15,0,0" Foreground="#1976D2"/>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Today Orders -->
                <materialDesign:Card Margin="0,0,15,0" Padding="20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Background="#E8F5E9" CornerRadius="8" Padding="10">
                                <materialDesign:PackIcon Kind="ShoppingCart" Width="24" Height="24" Foreground="#388E3C"/>
                            </Border>
                            <TextBlock Text="Đơn Hàng Hôm Nay" Foreground="Gray" VerticalAlignment="Center" Margin="12,0,0,0"/>
                        </StackPanel>
                        <TextBlock Text="{Binding TodaySalesCount}" FontSize="28" FontWeight="Bold" Margin="0,15,0,0" Foreground="#388E3C"/>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Total Products -->
                <materialDesign:Card Margin="0,0,15,0" Padding="20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Background="#FFF3E0" CornerRadius="8" Padding="10">
                                <materialDesign:PackIcon Kind="Package" Width="24" Height="24" Foreground="#F57C00"/>
                            </Border>
                            <TextBlock Text="Tổng Sản Phẩm" Foreground="Gray" VerticalAlignment="Center" Margin="12,0,0,0"/>
                        </StackPanel>
                        <TextBlock Text="{Binding TotalProducts}" FontSize="28" FontWeight="Bold" Margin="0,15,0,0" Foreground="#F57C00"/>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Low Stock Alert -->
                <materialDesign:Card Padding="20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Border Background="#FFEBEE" CornerRadius="8" Padding="10">
                                <materialDesign:PackIcon Kind="AlertCircle" Width="24" Height="24" Foreground="#D32F2F"/>
                            </Border>
                            <TextBlock Text="Sắp Hết Hàng" Foreground="Gray" VerticalAlignment="Center" Margin="12,0,0,0"/>
                        </StackPanel>
                        <TextBlock Text="{Binding LowStockCount}" FontSize="28" FontWeight="Bold" Margin="0,15,0,0" Foreground="#D32F2F"/>
                    </StackPanel>
                </materialDesign:Card>
            </UniformGrid>

            <!-- Charts Row -->
            <Grid Margin="0,0,0,25">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Revenue Chart (Last 7 Days) -->
                <materialDesign:Card Grid.Column="0" Margin="0,0,15,0" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="250"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="📊 Doanh Thu 7 Ngày Qua" FontSize="18" FontWeight="Medium" Margin="0,0,0,15"/>
                        
                        <lvc:CartesianChart Grid.Row="1" Series="{Binding RevenueSeries}" LegendLocation="None">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Labels="{Binding RevenueLabels}"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis LabelFormatter="{Binding YFormatter}"/>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Grid>
                </materialDesign:Card>

                <!-- Top Products Chart -->
                <materialDesign:Card Grid.Column="1" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="250"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="🏆 Top 5 Sản Phẩm Bán Chạy" FontSize="18" FontWeight="Medium" Margin="0,0,0,15"/>
                        
                        <lvc:PieChart Grid.Row="1" 
                                      Series="{Binding TopProductsSeries}" 
                                      LegendLocation="Right"
                                      InnerRadius="50"/>
                    </Grid>
                </materialDesign:Card>
            </Grid>

            <!-- Content Grid -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="350"/>
                </Grid.ColumnDefinitions>

                <!-- Recent Sales -->
                <materialDesign:Card Grid.Column="0" Margin="0,0,15,0" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="300"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Đơn Hàng Gần Đây" FontSize="18" FontWeight="Medium" Margin="0,0,0,15"/>

                        <DataGrid Grid.Row="1" 
                                  ItemsSource="{Binding RecentSales}"
                                  AutoGenerateColumns="False"
                                  IsReadOnly="True"
                                  CanUserAddRows="False"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="Horizontal"
                                  BorderThickness="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã HĐ" Binding="{Binding InvoiceNumber}" Width="120"/>
                                <DataGridTextColumn Header="Khách Hàng" Binding="{Binding CustomerName, TargetNullValue='Khách lẻ'}" Width="*"/>
                                <DataGridTextColumn Header="Tổng Tiền" Binding="{Binding TotalAmount, StringFormat='{}{0:N0} đ'}" Width="120"/>
                                <DataGridTextColumn Header="Ngày" Binding="{Binding SaleDate, StringFormat='HH:mm dd/MM'}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </materialDesign:Card>

                <!-- Low Stock Products -->
                <materialDesign:Card Grid.Column="1" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="300"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Sản Phẩm Sắp Hết" FontSize="18" FontWeight="Medium" Margin="0,0,0,15"/>

                        <ListBox Grid.Row="1" ItemsSource="{Binding LowStockProducts}" BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="5,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding Name}" FontWeight="Medium"/>
                                                <TextBlock Text="{Binding ProductCode}" Foreground="Gray" FontSize="12"/>
                                            </StackPanel>
                                            <Border Grid.Column="1" Background="#FFEBEE" CornerRadius="12" Padding="8,4">
                                                <TextBlock Text="{Binding StockQuantity, StringFormat='Còn {0}'}" Foreground="#D32F2F" FontSize="12"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </materialDesign:Card>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
