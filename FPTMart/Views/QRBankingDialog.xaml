<Window x:Class="FPTMart.Views.QRBankingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Chuyển Khoản Ngân Hàng" 
        Height="550" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <materialDesign:PackIcon Kind="QrcodeScan" Width="32" Height="32" Foreground="#1976D2"/>
            <TextBlock Text="Chuyển Khoản" FontSize="24" FontWeight="Medium" Margin="15,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Content -->
        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
            <!-- Amount -->
            <Border Background="#E3F2FD" CornerRadius="10" Padding="15" Margin="0,0,0,20">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Số tiền cần thanh toán" Foreground="#1976D2" FontSize="14" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="AmountText" 
                               FontSize="28" 
                               FontWeight="Bold" 
                               Foreground="#1976D2"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- QR Code -->
            <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="10" Padding="10" Background="#FAFAFA">
                <Image x:Name="QRImage" 
                       Width="250" Height="250" 
                       Stretch="Uniform"/>
            </Border>

            <!-- Instructions -->
            <TextBlock Text="Quét mã QR để chuyển khoản" 
                       Foreground="Gray" 
                       FontSize="14" 
                       HorizontalAlignment="Center"
                       Margin="0,15,0,5"/>
            <TextBlock Text="Nhấn 'Đã Chuyển' sau khi hoàn tất" 
                       Foreground="Gray" 
                       FontSize="12" 
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Hủy" 
                    Click="Cancel_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,15,0"
                    Width="120"/>
            <Button Content="Đã Chuyển" 
                    Click="Confirm_Click"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="#388E3C"
                    Width="120"/>
        </StackPanel>
    </Grid>
</Window>
