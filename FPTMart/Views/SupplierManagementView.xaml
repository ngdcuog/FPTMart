<UserControl x:Class="FPTMart.Views.SupplierManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <materialDesign:PackIcon Kind="TruckDelivery" Width="32" Height="32" VerticalAlignment="Center" Foreground="#1976D2"/>
                <TextBlock Text="Quản Lý Nhà Cung Cấp" FontSize="24" FontWeight="Medium" Margin="15,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            
            <Button Grid.Column="1" 
                    Command="{Binding AddSupplierCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Background="#1976D2">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center"/>
                    <TextBlock Text="Thêm NCC" Margin="8,0,0,0"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Content -->
        <materialDesign:Card Grid.Row="1" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Search -->
                <TextBox Grid.Row="0"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Tìm kiếm nhà cung cấp..."
                         materialDesign:TextFieldAssist.HasLeadingIcon="True"
                         materialDesign:TextFieldAssist.LeadingIcon="Magnify"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,0,15"/>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding Suppliers}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Tên NCC" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Người Liên Hệ" Binding="{Binding ContactPerson}" Width="130"/>
                        <DataGridTextColumn Header="Số Điện Thoại" Binding="{Binding Phone}" Width="110"/>
                        <DataGridTemplateColumn Header="Thao Tác" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                                Command="{Binding DataContext.EditSupplierCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                ToolTip="Sửa"
                                                Width="32" Height="32">
                                            <materialDesign:PackIcon Kind="Pencil" Foreground="#1976D2" Width="18" Height="18"/>
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                                Command="{Binding DataContext.DeleteSupplierCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                ToolTip="Xóa"
                                                Width="32" Height="32">
                                            <materialDesign:PackIcon Kind="Delete" Foreground="#F44336" Width="18" Height="18"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <!-- Add/Edit Dialog Overlay -->
        <Border Grid.RowSpan="2" 
                Background="#80000000" 
                Visibility="{Binding IsDialogOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
            <materialDesign:Card Width="450" Padding="25" VerticalAlignment="Center" HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock Text="{Binding DialogTitle}" FontSize="20" FontWeight="Medium" Margin="0,0,0,20"/>
                    
                    <TextBox Text="{Binding EditingSupplier.Name, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Tên nhà cung cấp *"
                             materialDesign:HintAssist.IsFloating="True"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,0,15"/>
                    
                    <TextBox Text="{Binding EditingSupplier.ContactPerson, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Người liên hệ"
                             materialDesign:HintAssist.IsFloating="True"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,0,15"/>
                    
                    <TextBox Text="{Binding EditingSupplier.Phone, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Số điện thoại"
                             materialDesign:HintAssist.IsFloating="True"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,0,20"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <Button Grid.Column="0" Content="Hủy" 
                                Command="{Binding CancelDialogCommand}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Margin="0,0,10,0"/>
                        <Button Grid.Column="1" Content="Lưu" 
                                Command="{Binding SaveSupplierCommand}"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                Background="#1976D2"/>
                    </Grid>
                </StackPanel>
            </materialDesign:Card>
        </Border>
    </Grid>
</UserControl>
